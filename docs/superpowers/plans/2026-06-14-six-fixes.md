# 批次五：六项修复与改进 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development（推荐）or superpowers:executing-plans 逐任务实现。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 会话删除入口、目标模式移入配置开关、记录一键清空、codegen 禁真实跑数只拿列、质检空内容误通过修复、节点进度条实时化。

**Architecture:** 后端 FastAPI + SQLAlchemy 2 async（SQLite，`create_all` 只建新表）；本批**不加新表、不加列**，只改逻辑/端点。前端 React 19 + antd 6 + TS。进度走既有 SSE（`events.publish`）+ antd `Progress`。codegen 改为只读列名（数据集 `columns_json` / 上游 RunRow 键），不再子进程执行。

**Tech Stack:** Python3 / FastAPI / SQLAlchemy async / pytest + httpx ASGITransport；React19 / antd6 / vitest。

**贯穿约束（KISS 硬规则）：** 最简实现，不预防未发生的 bug；api_key 全程 Fernet 加密、绝不进响应/日志/提示词；所有模型/工作流/运行/数据集引用校验 `user_id`。提交两个 `-m`，第二个 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。**禁止 `git add`** `项目设计.txt`、`.idea/`、`.codegraph/`。PowerShell 无 `&&`，用 Bash 工具 `cd backend && ...` 或分开跑。

**测试约定：** 后端 `cd backend; python -m pytest <path> -v`。前端 `cd frontend; npx vitest run` 与 `npm run build`。

**文件结构总览：**
- 改后端：`routers/runs.py`（批量删 runs）、`routers/agent.py`（批量删 sessions + codegen/node-assist 端点改列名）、`agent/codegen.py`（列名采集、去执行）、`engine/nodes.py`（质检兜底+温度0+锚定）、`engine/runner.py`（`_set_node_state` 推进度）。
- 改前端：`agent/AgentDrawer.tsx`（会话删除+删全部+目标模式开关）、`pages/RunsPage.tsx`（清空全部）、`canvas/forms/NodeConfigForm.tsx`（展示列名）、`pages/RunDetailPage.tsx`（SSE 进度+视觉）、`api/types.ts`（CodegenOut）。

---

# F1 · 批量清空记录（item ③）

## Task 1: 后端批量清空 API（runs + sessions）

**Files:**
- Modify: `backend/app/routers/runs.py`（加 `DELETE ""`，复用单删级联）
- Modify: `backend/app/routers/agent.py`（加 `DELETE "/sessions"`）
- Test: `backend/tests/test_bulk_delete.py`（新建）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_bulk_delete.py`：
```python
import json
from sqlalchemy import select
from app.models import (User, Workflow, WorkflowVersion, Run, RunRow, RunLog,
                        QcMetric, AgentSession, AgentMessage)


async def _mk_run(s, uid, wf_id, ver_id, status):
    r = Run(user_id=uid, workflow_id=wf_id, workflow_version_id=ver_id, status=status)
    s.add(r); await s.flush()
    s.add(RunRow(run_id=r.id, node_id="n", row_idx=0, status="done"))
    s.add(RunLog(run_id=r.id, message="x"))
    s.add(QcMetric(run_id=r.id, node_id="qc", total=1, first_round_pass=1))
    return r.id


async def test_delete_all_runs_skips_active_and_scopes_user(auth_client, session_factory):
    async with session_factory() as s:
        uid = (await s.execute(select(User).where(User.username == "tester"))).scalar_one().id
        other = User(username="other"); s.add(other); await s.flush()
        wf = Workflow(user_id=uid, name="w"); s.add(wf); await s.flush()
        ver = WorkflowVersion(workflow_id=wf.id, version=1, graph_json="{}"); s.add(ver); await s.flush()
        done_id = await _mk_run(s, uid, wf.id, ver.id, "completed")
        running_id = await _mk_run(s, uid, wf.id, ver.id, "running")
        owf = Workflow(user_id=other.id, name="ow"); s.add(owf); await s.flush()
        over = WorkflowVersion(workflow_id=owf.id, version=1, graph_json="{}"); s.add(over); await s.flush()
        other_id = await _mk_run(s, other.id, owf.id, over.id, "completed")
        await s.commit()
    r = await auth_client.delete("/api/runs")
    assert r.status_code == 200 and r.json()["deleted"] == 1
    async with session_factory() as s:
        ids = {x for (x,) in (await s.execute(select(Run.id))).all()}
        rows = (await s.execute(select(RunRow).where(RunRow.run_id == done_id))).scalars().all()
    assert done_id not in ids and running_id in ids and other_id in ids
    assert rows == []


async def test_delete_all_sessions(auth_client, session_factory):
    async with session_factory() as s:
        uid = (await s.execute(select(User).where(User.username == "tester"))).scalar_one().id
        for _ in range(2):
            sess = AgentSession(user_id=uid, models_json=json.dumps({"coordinator": 1}))
            s.add(sess); await s.flush()
            s.add(AgentMessage(session_id=sess.id, role="user", content_json="{}"))
        other = User(username="other2"); s.add(other); await s.flush()
        osess = AgentSession(user_id=other.id, models_json="{}"); s.add(osess); await s.flush()
        await s.commit(); other_sid = osess.id
    r = await auth_client.delete("/api/agent/sessions")
    assert r.status_code == 200 and r.json()["deleted"] == 2
    async with session_factory() as s:
        remaining = {x for (x,) in (await s.execute(select(AgentSession.id))).all()}
    assert remaining == {other_sid}
```

- [ ] **Step 2: 跑测试看失败**

Run: `cd backend; python -m pytest tests/test_bulk_delete.py -v`
Expected: FAIL（端点不存在，405/404）

- [ ] **Step 3: 实现 runs 批量删**

`backend/app/routers/runs.py`：在 `delete_run`（`DELETE /{run_id}`）**之前**加一个无路径段的批量端点（复用同样的级联模型与导出清理）：
```python
@router.delete("")
async def delete_all_runs(user: User = Depends(get_current_user),
                          session: AsyncSession = Depends(get_session)):
    runs = (await session.execute(select(Run).where(
        Run.user_id == user.id, Run.status.notin_(("queued", "running"))))).scalars().all()
    run_ids = [r.id for r in runs]
    ver_ids = [r.workflow_version_id for r in runs]
    if run_ids:
        for Model in (RunRow, RunNodeState, RunLog, QcMetric, QcFailure):
            await session.execute(sa_delete(Model).where(Model.run_id.in_(run_ids)))
        await session.execute(sa_delete(Run).where(Run.id.in_(run_ids)))
        await session.execute(sa_delete(WorkflowVersion).where(WorkflowVersion.id.in_(ver_ids)))
        await session.commit()
        for rid in run_ids:
            for p in (settings.data_dir / "exports").glob(f"run{rid}_*"):
                p.unlink(missing_ok=True)
    return {"deleted": len(run_ids)}
```
（`select`、`sa_delete`、`RunRow/RunNodeState/RunLog/QcMetric/QcFailure/Run/WorkflowVersion`、`settings` 均已在 `delete_run` 处 import；无需新增。若 `select` 未导入则补 `from sqlalchemy import select`。）

- [ ] **Step 4: 实现 sessions 批量删**

`backend/app/routers/agent.py`：在 `delete_session`（`DELETE /sessions/{sid}`）**之前**加：
```python
@router.delete("/sessions")
async def delete_all_sessions(user: User = Depends(get_current_user),
                              session: AsyncSession = Depends(get_session)):
    sessions = (await session.execute(select(AgentSession).where(
        AgentSession.user_id == user.id))).scalars().all()
    sids = [s.id for s in sessions]
    for sid in sids:
        turn_manager.cancel(sid)
    if sids:
        await session.execute(sa_delete(AgentMessage).where(AgentMessage.session_id.in_(sids)))
        await session.execute(sa_delete(AgentSession).where(AgentSession.id.in_(sids)))
        await session.commit()
        for sid in sids:
            shutil.rmtree(session_dir(user.username, sid), ignore_errors=True)
    return {"deleted": len(sids)}
```
（`select`、`sa_delete`、`AgentSession`、`AgentMessage`、`turn_manager`、`shutil`、`session_dir` 均已在 `delete_session` 处可用。）

- [ ] **Step 5: 跑测试看通过**

Run: `cd backend; python -m pytest tests/test_bulk_delete.py -v`
Expected: PASS（2 passed）

- [ ] **Step 6: 提交**

```bash
git add backend/app/routers/runs.py backend/app/routers/agent.py backend/tests/test_bulk_delete.py
git commit -m "feat(records): 运行/会话 per-user 批量清空 API" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# F2 · AgentDrawer：会话删除 + 删全部 + 目标模式开关（item ①②③前端）

## Task 2: AgentDrawer 改造

**Files:**
- Modify: `frontend/src/agent/AgentDrawer.tsx`
- Test: `cd frontend; npm run build`

> 背景（现状已核实）：标题栏 `Space`（会话 Select + 新建 + 模型 Select + 高级）在 line 181-191；advanced 角色块 line 193-201；底部绝对定位区 line 209-247，其中目标启动条在 216-225、指标历史 226-234、发送框 235-246。state hooks 在 14-30（含 `goalText/goalWf/workflows/metrics`），`startGoal` 在 121-130，`selectSession` 在 36-43。

- [ ] **Step 1: 新增「目标模式开关」state**

在 state hooks 区（约 line 24，`goalRound` 旁）加：
```tsx
  const [goalMode, setGoalMode] = useState(false)
```
并在顶部 antd import 补 `Switch`、`Popconfirm`（确认是否已 import；`Switch`/`Popconfirm` 现未 import，需加入 `import { ... } from 'antd'`）。

- [ ] **Step 2: 会话单条删除 + 删除全部会话**

加两个处理函数（放在 `selectSession` 附近）：
```tsx
  const deleteSession = async (sid: number) => {
    await api.del(`/api/agent/sessions/${sid}`)
    const rest = sessions.filter((x) => x.id !== sid)
    setSessions(rest)
    if (sessionIdRef.current === sid) {
      sessionIdRef.current = null
      if (rest.length) await selectSession(rest[0].id)
      else setDetail(null)
    }
  }
  const deleteAllSessions = async () => {
    await api.del('/api/agent/sessions')
    setSessions([]); sessionIdRef.current = null; setDetail(null)
    message.success('已清空会话')
  }
```

- [ ] **Step 3: 标题栏加删除入口与目标模式开关**

把标题 `Space`（line 181-191）末尾追加（在「高级」按钮后）：
```tsx
                  <Popconfirm title="删除当前会话？" disabled={!detail}
                              onConfirm={() => detail && void deleteSession(detail.id)}>
                    <Button size="small" danger disabled={!detail}>删除会话</Button>
                  </Popconfirm>
                  <Popconfirm title="清空全部会话？" onConfirm={() => void deleteAllSessions()}>
                    <Button size="small" danger>清空全部</Button>
                  </Popconfirm>
                  <span style={{ fontSize: 12 }}>目标模式
                    <Switch size="small" style={{ marginLeft: 4 }} checked={goalMode}
                            onChange={setGoalMode} /></span>
```

- [ ] **Step 4: 目标启动条从底部移到配置区（开关控制）**

删除底部那段 `{detail && !running && (<Space.Compact …目标工作流…目标模式 Button…/>)}`（现 line 216-225）。改为在 advanced 角色块（line 193-201）**之后**插入一段「目标模式配置行」，仅当开关开启且有会话且未运行时显示：
```tsx
        {goalMode && detail && !running && (
          <Space style={{ marginBottom: 8 }} wrap>
            <Select size="small" style={{ width: 160 }} placeholder="目标工作流"
                    value={goalWf} onChange={setGoalWf}
                    options={workflows.map((w) => ({ value: w.id, label: w.name }))} />
            <Input size="small" style={{ width: 280 }}
                   placeholder="一句话目标，如：把首轮质检通过率提到 90%"
                   value={goalText} onChange={(e) => setGoalText(e.target.value)} />
            <Button size="small" type="primary" onClick={() => void startGoal()}>启动目标</Button>
          </Space>
        )}
```
（`startGoal`、`goalText/goalWf/workflows` 保持不变；指标历史块 226-234 与运行中提示 210-215 保留在底部。）

- [ ] **Step 5: 构建验证**

Run: `cd frontend; npm run build`
Expected: 构建成功，无 TS 错误（确认无未用 import）

- [ ] **Step 6: 提交**

```bash
git add frontend/src/agent/AgentDrawer.tsx
git commit -m "feat(agent): 会话删除/清空 + 目标模式移入配置开关" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: RunsPage 清空全部运行

**Files:**
- Modify: `frontend/src/pages/RunsPage.tsx`
- Test: `cd frontend; npm run build`

- [ ] **Step 1: 表格上方加「清空全部」**

`RunsPage.tsx`：把 `return (<Table … />)` 包一层，表格上方加按钮条：
```tsx
  return (
    <>
      <Popconfirm title="清空全部运行记录（运行中的除外）？"
                  onConfirm={async () => {
                    const r = await api.del<{ deleted: number }>('/api/runs')
                    message.success(`已清空 ${r.deleted} 条`); await reload()
                  }}>
        <Button danger size="small" style={{ marginBottom: 12 }}>清空全部</Button>
      </Popconfirm>
      <Table /* …原样… */ />
    </>
  )
```
（`Button`/`Popconfirm`/`message` 已在 line 2 import；`api.del` 支持泛型返回。）

- [ ] **Step 2: 构建验证**

Run: `cd frontend; npm run build`
Expected: 构建成功

- [ ] **Step 3: 提交**

```bash
git add frontend/src/pages/RunsPage.tsx
git commit -m "feat(records): 运行列表清空全部按钮" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# F3 · codegen 禁真实跑数，只拿列名（item ④）

## Task 4: 后端 codegen 列名采集 + 去执行

**Files:**
- Modify: `backend/app/agent/codegen.py`
- Modify: `backend/app/routers/agent.py`（codegen / node-assist 端点）
- Test: `backend/tests/test_codegen_columns.py`（新建）；改既有 `backend/tests/test_codegen.py`（若有断言 preview/执行的用例需同步）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_codegen_columns.py`：
```python
import json
from sqlalchemy import select
from app.agent.codegen import gather_upstream_columns
from app.models import User, Workflow, Dataset, DatasetRow


async def test_gather_upstream_columns_from_dataset(auth_client, session_factory):
    async with session_factory() as s:
        uid = (await s.execute(select(User).where(User.username == "tester"))).scalar_one().id
        ds = Dataset(user_id=uid, name="d", columns_json=json.dumps(["q", "category"]))
        s.add(ds); await s.flush()
        s.add(DatasetRow(dataset_id=ds.id, idx=0, data_json=json.dumps({"q": "你好", "category": "x"})))
        graph = {"nodes": [{"id": "in", "type": "input", "config": {"dataset_ids": [ds.id]}},
                           {"id": "ap", "type": "auto_process", "config": {}}],
                 "edges": [{"source": "in", "target": "ap", "kind": "normal"}]}
        wf = Workflow(user_id=uid, name="w", graph_json=json.dumps(graph))
        s.add(wf); await s.commit(); wf_id = wf.id
        async with session_factory() as s2:
            cols, source = await gather_upstream_columns(s2, wf_id, "ap", uid)
    assert cols == ["q", "category"] and source == "dataset"


async def test_codegen_endpoint_returns_columns_no_preview(auth_client, session_factory, monkeypatch):
    # 桩掉 LLM 代码生成，确认端点返回 columns 且无 preview_rows、不执行
    async def fake_generate_code(model, instruction, columns):
        return "def process(rows):\n    return rows"
    monkeypatch.setattr("app.routers.agent.generate_code", fake_generate_code)
    async with session_factory() as s:
        uid = (await s.execute(select(User).where(User.username == "tester"))).scalar_one().id
        from app.models import ModelConfig
        mc = ModelConfig(user_id=uid, name="m", base_url="http://x", api_key_enc="")
        ds = Dataset(user_id=uid, name="d", columns_json=json.dumps(["a"]))
        s.add_all([mc, ds]); await s.flush()
        graph = {"nodes": [{"id": "in", "type": "input", "config": {"dataset_ids": [ds.id]}},
                           {"id": "ap", "type": "auto_process", "config": {}}],
                 "edges": [{"source": "in", "target": "ap", "kind": "normal"}]}
        wf = Workflow(user_id=uid, name="w", graph_json=json.dumps(graph))
        s.add(wf); await s.commit(); wf_id, mid = wf.id, mc.id
    r = await auth_client.post("/api/agent/codegen", json={
        "workflow_id": wf_id, "node_id": "ap", "instruction": "删空行", "model_config_id": mid})
    assert r.status_code == 200
    body = r.json()
    assert body["columns"] == ["a"] and "preview_rows" not in body and body["code"].startswith("def process")
```

- [ ] **Step 2: 跑测试看失败**

Run: `cd backend; python -m pytest tests/test_codegen_columns.py -v`
Expected: FAIL（`gather_upstream_columns`/`generate_code` 不存在）

- [ ] **Step 3: 改 codegen.py —— 列名采集 + 去执行**

`backend/app/agent/codegen.py`：
1. 删除 `from app.engine.pycode import run_process_code` 这一行（不再执行）。
2. `INSTRUCTIONS` 末段「常见模式」保留，但把开头与 `_user_prompt` 的「样本行」语义改为「上游可用列」。把 `_user_prompt` 改为：
```python
def _user_prompt(instruction: str, columns: list[str]) -> str:
    cols = "、".join(columns) if columns else "（未知，按指令中提到的列名处理）"
    return f"用户指令：{instruction}\n\n上游可用列：{cols}"
```
3. 用 `generate_code` 取代 `generate_with_repair`（删除后者及其试跑/修复循环）：
```python
async def generate_code(model, instruction: str, columns: list[str]) -> str:
    """只按指令+上游列名生成处理函数源码；不执行、不预览。"""
    agent = create_agent(model, [], INSTRUCTIONS)
    result = await agent.run(_user_prompt(instruction, columns))
    return strip_code_fences(str(result.output or ""))
```
4. `generate_node_config` 形参 `sample_rows` 改名 `columns`（内部调用 `_user_prompt(instruction, columns)`，其余不变）。
5. 用 `gather_upstream_columns` 取代 `gather_sample_rows`（保留 `_upstream_run_rows`，新增列工具与数据集列遍历）：
```python
async def gather_upstream_columns(s: AsyncSession, workflow_id: int, node_id: str, user_id: int):
    """取上游节点产出的列名（只名不值）。优先最近一次运行的上游输出键，否则上游 input 数据集列。
    返回 (columns, source)。"""
    run = (await s.execute(select(Run).where(Run.workflow_id == workflow_id)
                           .order_by(Run.id.desc()))).scalars().first()
    if run is not None:
        ver = await s.get(WorkflowVersion, run.workflow_version_id)
        rows = await _upstream_run_rows(s, run.id, parse_graph(ver.graph_json), node_id)
        if rows:
            return _columns_of(rows), "last_run"
    wf = await s.get(Workflow, workflow_id)
    cols = await _upstream_dataset_columns(s, parse_graph(wf.graph_json), node_id, user_id)
    if cols:
        return cols, "dataset"
    return [], "none"


def _columns_of(rows: list[dict]) -> list[str]:
    cols: list[str] = []
    for r in rows:
        for k in r:
            if k not in cols and not k.startswith("_qc"):
                cols.append(k)
    return cols


async def _upstream_dataset_columns(s, graph: Graph, node_id: str, user_id: int) -> list[str]:
    """复用 _upstream_dataset_rows 的上游 input 遍历，但取数据集 columns_json。"""
    by_id = {n.id: n for n in graph.nodes}
    if node_id not in by_id:
        return []
    seen: set[str] = set()
    frontier, dataset_ids = [node_id], []
    while frontier:
        for uid in upstream_ids(graph, frontier.pop()):
            if uid in seen:
                continue
            seen.add(uid)
            frontier.append(uid)
            if by_id[uid].type == "input":
                dataset_ids.extend(by_id[uid].config.get("dataset_ids", []))
    cols: list[str] = []
    for ds_id in dataset_ids:
        ds = await s.get(Dataset, ds_id)
        if ds is None or ds.user_id != user_id:  # 资源归属校验（租户隔离）
            continue
        for c in json.loads(ds.columns_json):
            if c not in cols:
                cols.append(c)
    return cols
```
6. 删除 `_upstream_dataset_rows` 与 `gather_sample_rows`（不再使用）；保留 `_upstream_run_rows`。`DatasetRow` 若不再被引用则从 import 去掉，`SAMPLE_N` 仍被 `_upstream_run_rows` 使用故保留，`MAX_REPAIR_ROUNDS` 删除。

- [ ] **Step 4: 改端点**

`backend/app/routers/agent.py`：
- 顶部 import 把 `gather_sample_rows, generate_with_repair` 改为 `gather_upstream_columns, generate_code`（`codegen_mod.generate_node_config` 仍用）。
- `codegen` 端点末两行改为：
```python
    columns, source = await gather_upstream_columns(session, body.workflow_id, body.node_id, user.id)
    code = await generate_code(mc, body.instruction, columns)
    return {"code": code, "columns": columns, "sample_source": source}
```
- `node_assist` 端点末两行改为：
```python
    columns, source = await gather_upstream_columns(session, body.workflow_id, body.node_id, user.id)
    config = await codegen_mod.generate_node_config(mc, body.node_type, body.instruction, columns)
    return {"config": config, "sample_source": source}
```

- [ ] **Step 5: 跑测试 + 同步既有 codegen 测试**

Run: `cd backend; python -m pytest tests/test_codegen_columns.py tests/test_codegen.py -v`
Expected: 新测试 PASS；既有 `test_codegen.py` 若有断言 `preview_rows`/执行行为的用例，改为断言 `columns`/不执行（按新契约最小改动，删掉对真跑预览的断言）。

- [ ] **Step 6: 提交**

```bash
git add backend/app/agent/codegen.py backend/app/routers/agent.py backend/tests/test_codegen_columns.py backend/tests/test_codegen.py
git commit -m "fix(codegen): 只采上游列名、禁止真实跑数与数据预览" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: 前端 codegen/node-assist 展示列名

**Files:**
- Modify: `frontend/src/canvas/forms/NodeConfigForm.tsx`
- Modify: `frontend/src/api/types.ts`
- Test: `cd frontend; npm run build`

- [ ] **Step 1: 改类型**

`frontend/src/api/types.ts`：`CodegenOut` 改为：
```ts
export interface CodegenOut {
  code: string
  columns: string[]
  sample_source: 'last_run' | 'dataset' | 'none'
}
```
（`NodeAssistOut` 不变。）

- [ ] **Step 2: 改 AgentOpFields（生成代码）**

`NodeConfigForm.tsx` `AgentOpFields`：
- `const [preview, setPreview] = useState<…>(null)` 改为 `const [cols, setCols] = useState<string[]>([])`。
- `generate()` 内：
```tsx
      const r = await api.post<CodegenOut>('/api/agent/codegen', {
        workflow_id: workflowId, node_id: nodeId,
        instruction: op.instruction, model_config_id: modelSel,
      })
      update({ code: r.code })
      setCols(r.columns)
      if (r.sample_source === 'none') setInfo('未检测到上游列（先连好上游/上传数据集），AI 仅按指令生成')
```
- 渲染处把数据 `<pre>` 块替换为列名提示：
```tsx
      {cols.length > 0 && (
        <div style={{ fontSize: 12, color: '#555', marginTop: 4 }}>检测到的上游列：{cols.join('、')}</div>
      )}
```

- [ ] **Step 3: 改 NodeAssist 文案（仅提示语，不再暗示真实数据）**

`NodeAssist.run()` 里 `setInfo('没有可用样本…')` 改为 `setInfo('未检测到上游列，可先连好上游')`；顶部说明「描述需求，自动写提示词」保留。

- [ ] **Step 4: 构建验证**

Run: `cd frontend; npm run build`
Expected: 构建成功（确认无 `preview`/`CodegenOut.preview_rows` 残留引用）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/canvas/forms/NodeConfigForm.tsx frontend/src/api/types.ts
git commit -m "fix(codegen): 前端展示检测到的上游列名，去掉数据预览" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# F4 · 质检空内容误通过修复（item ⑤）

## Task 6: run_qc_judge_row 兜底 + 锚定 + 温度0

**Files:**
- Modify: `backend/app/engine/nodes.py`（`run_qc_judge_row`）
- Test: `backend/tests/test_qc_empty.py`（新建）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_qc_empty.py`：
```python
import pytest
from app.engine import nodes


async def test_empty_sample_fails_without_judge(monkeypatch):
    called = False
    async def boom(*a, **k):
        nonlocal called; called = True
        raise AssertionError("judge 不应被调用")
    monkeypatch.setattr(nodes.llm, "chat", boom)
    import asyncio
    sem = asyncio.Semaphore(4)
    # 空行 + 全空白行都应直接判失败，且不调用 judge
    for row in ({}, {"q_en": "", "category_en": "   "}):
        ok, reason, usage, per_model = await nodes.run_qc_judge_row(
            {"system_prompt": "判断", "user_prompt": "{{q_en}}"}, row, [object()], 1, sem)
        assert ok is False and "空" in reason and per_model == []
    assert called is False


async def test_judge_uses_temperature_zero(monkeypatch):
    seen = {}
    async def fake_chat(mc, system, user, params=None, retries=3):
        seen.update(params or {}); seen["system"] = system
        return '{"pass": true, "reason": "ok"}', {"prompt_tokens": 1, "completion_tokens": 1}
    monkeypatch.setattr(nodes.llm, "chat", fake_chat)
    import asyncio
    class MC: id = 7
    ok, *_ = await nodes.run_qc_judge_row(
        {"system_prompt": "判断", "user_prompt": "{{q}}"}, {"q": "非空内容"}, [MC()], 1,
        asyncio.Semaphore(4))
    assert ok is True
    assert seen.get("temperature") == 0
    assert "pass:false" in seen["system"]  # 锚定句已追加
```

- [ ] **Step 2: 跑测试看失败**

Run: `cd backend; python -m pytest tests/test_qc_empty.py -v`
Expected: FAIL（judge 被调用 / 无 temperature / 无锚定句）

- [ ] **Step 3: 改 run_qc_judge_row**

`backend/app/engine/nodes.py` `run_qc_judge_row`（约 line 160-189）。在构造 `base` 之后、判定之前加兜底；并改 `system`/`params`：
```python
    base = {k: v for k, v in row.items() if not k.startswith("_qc")}
    if not any(str(v).strip() for v in base.values()):   # 空/全空白样本：直接判不通过，不调 judge
        return False, "样本内容为空", {"prompt_tokens": 0, "completion_tokens": 0}, []
    system = render_template(config.get("system_prompt", ""), base) + \
        "\n\n硬性规则：若待判定内容为空或缺少必要字段，必须返回 pass:false。"
    user = render_template(config.get("user_prompt", ""), base)
    params = {**config.get("params", {}), "json_mode": True, "temperature": 0}
```
其余（`judge_one`、聚合、`n_pass >= pass_k`）不变。

- [ ] **Step 4: 跑测试看通过 + 回归既有质检测试**

Run: `cd backend; python -m pytest tests/test_qc_empty.py tests/test_qc.py tests/test_qc_multi.py -v`
Expected: 全 PASS（既有质检用例的样本非空，行为不变）

- [ ] **Step 5: 提交**

```bash
git add backend/app/engine/nodes.py backend/tests/test_qc_empty.py
git commit -m "fix(qc): 空样本直接判失败 + judge 锚定非空 + 温度固定0" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# F5 · 节点进度条实时化（item ⑥）

## Task 7: 后端 _set_node_state 推 progress 事件

**Files:**
- Modify: `backend/app/engine/runner.py`（`_set_node_state` 加 `user_id` 形参 + publish；更新全部调用点）
- Test: `backend/tests/test_progress_event.py`（新建）

- [ ] **Step 1: 写失败测试**

`backend/tests/test_progress_event.py`：
```python
import json
from app import events
from app.engine.runner import _set_node_state


async def test_set_node_state_publishes_progress(session_factory):
    q = events.subscribe(1)
    try:
        await _set_node_state(session_factory, 1, "n1", user_id=1,
                              status="running", total=10, done=3, failed=0)
        payload = json.loads(q.get_nowait())
    finally:
        events.unsubscribe(1, q)
    assert payload["entity"] == "run" and payload["id"] == 1 and payload["kind"] == "progress"
    assert payload["data"] == {"node_id": "n1", "status": "running",
                               "total": 10, "done": 3, "failed": 0}
```

- [ ] **Step 2: 跑测试看失败**

Run: `cd backend; python -m pytest tests/test_progress_event.py -v`
Expected: FAIL（`_set_node_state` 无 `user_id` 形参 / 无 publish）

- [ ] **Step 3: 改 _set_node_state + 全部调用点**

`backend/app/engine/runner.py`：`_set_node_state` 增加 `user_id` 关键字形参，写库后 publish（`publish` 已在文件顶部 import）：
```python
async def _set_node_state(session_factory, run_id, node_id, *, user_id, status, total, done, failed):
    async with session_factory() as s:
        ns = (await s.execute(select(RunNodeState).where(
            RunNodeState.run_id == run_id, RunNodeState.node_id == node_id
        ))).scalar_one_or_none()
        if ns is None:
            ns = RunNodeState(run_id=run_id, node_id=node_id)
            s.add(ns)
        ns.status, ns.total, ns.done, ns.failed = status, total, done, failed
        await s.commit()
    publish(user_id, "run", run_id, kind="progress",
            data={"node_id": node_id, "status": status, "total": total, "done": done, "failed": failed})
```
然后给**所有** `_set_node_state(...)` 调用补 `user_id=user_id`。调用点（均已有 `user_id` 在作用域）：
- `_run_barrier_node`：line 176、178、183、186
- `_run_llm_node`：line 224-225、246-247、251-252
- `_run_qc_node`：line 269-270、274-275、328-329、348-349

逐处把 `_set_node_state(session_factory, run_id, node.id, status=...` 改为 `_set_node_state(session_factory, run_id, node.id, user_id=user_id, status=...`。

- [ ] **Step 4: 跑测试看通过 + 全量回归（确认调用点没漏）**

Run: `cd backend; python -m pytest tests/test_progress_event.py tests/test_runner.py tests/test_qc.py -v`
Expected: 全 PASS（漏改任一调用点会因缺 `user_id` 立刻 TypeError 报错）

- [ ] **Step 5: 提交**

```bash
git add backend/app/engine/runner.py backend/tests/test_progress_event.py
git commit -m "feat(progress): 节点状态变更实时推送 SSE progress 事件" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: 前端 RunDetailPage 订阅实时进度 + 视觉优化

**Files:**
- Modify: `frontend/src/pages/RunDetailPage.tsx`
- Test: `cd frontend; npm run build`

> 现状：每节点已渲染 antd `Progress`（line 109-122）；运行中每 2 秒轮询 `refresh`（45-49）与 `refreshLogs`（26-30）。`useEvents` 未引入。

- [ ] **Step 1: 引入 useEvents，实时就地更新节点进度**

顶部 import 加 `import { useEvents } from '../api/events'`，并加 `NodeState` 类型 import（`import type { …, NodeState } from '../api/types'`）。在 `refresh` effect 后加：
```tsx
  useEvents((e) => {
    if (e.entity !== 'run' || e.id !== Number(id)) return
    if (e.kind === 'progress') {
      const d = e.data as NodeState
      setRun((r) => r && ({
        ...r,
        node_states: r.node_states.some((s) => s.node_id === d.node_id)
          ? r.node_states.map((s) => (s.node_id === d.node_id ? d : s))
          : [...r.node_states, d],
      }))
    } else void refresh()   // 运行完成等：整页刷新
  })
```
（2 秒轮询保留作兜底。）

- [ ] **Step 2: 顶部总进度条 + 节点卡片更清晰**

在节点卡片 `Space`（line 108）**之前**加一条总进度（仅运行中显示）：
```tsx
      {isActive && (() => {
        const tot = run.node_states.reduce((a, s) => a + s.total, 0)
        const dn = run.node_states.reduce((a, s) => a + s.done, 0)
        return <Progress percent={tot ? Math.round((dn / tot) * 100) : 0} status="active"
                         style={{ marginBottom: 12 }} />
      })()}
```
节点卡片（line 110-120）视觉增强：标题加粗、`Progress` 加 `percentPosition`/动效默认即平滑；把卡片宽度与 done/total 字号调清晰，例如：
```tsx
          <Card key={s.node_id} size="small" style={{ width: 230 }}>
            <div style={{ fontWeight: 600, marginBottom: 4 }}>{nodeLabel(s.node_id)}</div>
            <Progress percent={s.total ? Math.round((s.done / s.total) * 100) : 0}
                      status={s.failed > 0 ? 'exception' : s.status === 'done' ? 'success' : 'active'} />
            <div style={{ fontSize: 13 }}>{s.done}/{s.total}
              {s.failed > 0 && <span style={{ color: '#ff4d4f' }}>（失败 {s.failed}）</span>}</div>
          </Card>
```

- [ ] **Step 3: 构建验证**

Run: `cd frontend; npm run build`
Expected: 构建成功

- [ ] **Step 4: 提交**

```bash
git add frontend/src/pages/RunDetailPage.tsx
git commit -m "feat(progress): 运行详情实时进度（SSE）+ 总进度条与视觉优化" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: 全量回归

**Files:** 无（仅跑测试）

- [ ] **Step 1: 后端全量**

Run: `cd backend; python -m pytest -q`
Expected: 全绿。如有红：定位修复，不得跳过。

- [ ] **Step 2: 前端单测 + 构建**

Run: `cd frontend; npx vitest run; npm run build`
Expected: vitest 全过、build 成功

- [ ] **Step 3: 提交（若回归中有修复）**

```bash
git add -A -- backend frontend
git commit -m "test: 六项修复 全量回归通过" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 计划自审

**Spec 覆盖：** ① 会话删除→Task 2（后端已存在）；② 目标模式开关→Task 2；③ 批量清空→Task 1（后端）+Task 2/3（前端）；④ codegen 列名→Task 4（后端）+Task 5（前端）；⑤ 质检修复→Task 6；⑥ 进度条→Task 7（后端）+Task 8（前端）。✓

**类型/签名一致性：** `gather_upstream_columns(...) -> (list[str], str)`（Task 4 定义，Task 4 端点用）；`generate_code(model, instruction, columns)`（Task 4 定义，端点 + Task 4 测试用）；`CodegenOut {code, columns, sample_source}`（Task 5 前后端一致）；`_set_node_state(..., user_id=...)`（Task 7 定义并改全部调用点）；progress 事件 `{node_id,status,total,done,failed}`（Task 7 publish 与 Task 8 消费一致）。✓

**安全：** 批量删除均 `user_id` 作用域（Task 1）；codegen 列采集校验数据集归属（Task 4 `_upstream_dataset_columns`）；api_key 不变（仍仅 `llm.chat`/factory 内解密）。✓

**KISS：** 不加表不加列；质检兜底针对**已观测** bug（非投机）；进度复用既有 SSE + Progress；codegen 去掉执行而非新增抽象。✓
