我现在想做一个自动化的跑数平台，主要做成鼠标拖拽的节点式。类似Dify。
前端采用nodejs，后端采用FastAPI（UV管理）
前端页面要求：
1.目前先定义3种节点，调用LLM合成数据（需要system_prompt和 user prompt），调用LLM写代码处理数据和自动处理数据的。
2.额外的参数配置，比如并发、输入输出定义、模型选择、模型参数控制等等
后端要求：
1.支持用户会话隔离；
2.数据要落盘，可以使用sqlite
3.支持高并发

最后还需要支持数据质检、质检回扫等等功能
可以通过类似mermaid图的方式，快速构建

你需要问我20个问题，来完善这个方案